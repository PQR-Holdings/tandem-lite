{
  "name": "tandem-lite",
  "displayName": "Tandem Lite",
  "description": "Beta execution companion for reviewable developer and desktop automation on Windows and macOS, with local and hosted model support.",
  "version": "0.2.0",
  "publisher": "PQRHoldings",
  "author": "PQR LLC",
  "preview": true,
  "repository": { "type": "git", "url": "https://github.com/PQR-Holdings/tandem-lite.git" },
  "homepage": "https://github.com/PQR-Holdings/tandem-lite#readme",
  "bugs": { "url": "https://github.com/PQR-Holdings/tandem-lite/issues" },
  "icon": "media/icon.png",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "main": "./extension/extension.js",
  "engines": { "vscode": "^1.85.0" },
  "activationEvents": ["onView:developerAgent.sidebar", "onCommand:developerAgent.runObjective"],
  "categories": ["Other"],
  "contributes": {
    "viewsContainers": { "activitybar": [{ "id": "developerAgent", "title": "Tandem Lite", "icon": "media/agent.svg" }] },
    "views": { "developerAgent": [{ "id": "developerAgent.sidebar", "name": "Tandem Lite", "type": "webview" }] },
    "commands": [
      { "command": "developerAgent.runObjective", "title": "Tandem Lite: Run Objective" },
      { "command": "developerAgent.pause", "title": "Tandem Lite: Pause" },
      { "command": "developerAgent.resume", "title": "Tandem Lite: Resume" },
      { "command": "developerAgent.stop", "title": "Tandem Lite: Stop" },
      { "command": "developerAgent.showActivity", "title": "Tandem Lite: Show Activity" },
      { "command": "developerAgent.configureModel", "title": "Tandem Lite: Configure Model" },
      { "command": "developerAgent.setApiKey", "title": "Tandem Lite: Set Provider API Key" },
      { "command": "developerAgent.managePermissions", "title": "Tandem Lite: Manage Permissions" }
    ],
    "configuration": {
      "title": "Tandem Lite",
      "properties": {
        "developerAgent.model.provider": { "type": "string", "default": "ollama", "enum": ["ollama", "openai", "anthropic", "gemini", "openai-compatible"], "description": "Model provider." },
        "developerAgent.model.model": { "type": "string", "default": "qwen3:4b", "description": "Legacy planner-model setting. Prefer Tandem Lite: Planner Model for new configurations." },
        "developerAgent.model.planner": { "type": "string", "default": "qwen3:4b", "description": "Text model used to create plans. Recommended: qwen3:4b. Faster/lighter: qwen3:1.7b. More capable local option: qwen3.5:4b." },
        "developerAgent.model.visionFallback": { "type": "string", "default": "qwen3-vl:4b-instruct", "description": "Vision model reserved for screenshot-based UI fallback. Recommended: qwen3-vl:4b-instruct. This setting takes effect once visual observation is enabled." },
        "developerAgent.model.statusModel": { "type": "string", "default": "qwen3:1.7b", "description": "Small local model used asynchronously for short status and activity descriptions. Set to an empty string to use built-in wording only." },
        "developerAgent.model.planningTimeoutMs": { "type": "number", "default": 30000, "minimum": 3000, "maximum": 60000, "description": "Maximum time to wait for the planner model before using the deterministic fallback plan. 30,000 ms is recommended for a cold local qwen3:4b request." },
        "developerAgent.model.endpoint": { "type": "string", "default": "http://127.0.0.1:11434", "description": "Provider endpoint. API keys are stored in SecretStorage." },
        "developerAgent.providers.ollama.model": { "type": "string", "default": "qwen3:4b", "description": "Ollama planner model. Used whenever Tandem Lite: Model Provider is ollama." },
        "developerAgent.providers.ollama.endpoint": { "type": "string", "default": "http://127.0.0.1:11434", "description": "Ollama API endpoint. Used whenever the Ollama provider is selected." },
        "developerAgent.providers.openai.model": { "type": "string", "default": "gpt-5.6-terra", "description": "OpenAI planner model. Used whenever Tandem Lite: Model Provider is openai." },
        "developerAgent.providers.openai.endpoint": { "type": "string", "default": "https://api.openai.com/v1", "description": "OpenAI-compatible API endpoint. Used whenever the OpenAI provider is selected." },
        "developerAgent.providers.anthropic.model": { "type": "string", "default": "claude-sonnet-5", "description": "Anthropic planner model. Used whenever the Anthropic provider is selected." },
        "developerAgent.providers.anthropic.endpoint": { "type": "string", "default": "https://api.anthropic.com/v1", "description": "Anthropic API endpoint. Used whenever the Anthropic provider is selected." },
        "developerAgent.providers.gemini.model": { "type": "string", "default": "gemini-3.6-flash", "description": "Gemini planner model. Used whenever the Gemini provider is selected." },
        "developerAgent.providers.gemini.endpoint": { "type": "string", "default": "https://generativelanguage.googleapis.com/v1beta/openai", "description": "Gemini OpenAI-compatible API endpoint." },
        "developerAgent.providers.openai-compatible.model": { "type": "string", "default": "", "description": "OpenAI-compatible planner model. Used whenever that provider is selected." },
        "developerAgent.providers.openai-compatible.endpoint": { "type": "string", "default": "", "description": "OpenAI-compatible API endpoint. Used whenever that provider is selected." },
        "developerAgent.permissions.terminal": { "type": "string", "default": "ask", "enum": ["allow", "ask", "deny"], "description": "Terminal execution policy." },
        "developerAgent.permissions.windows": { "type": "string", "default": "ask", "enum": ["allow", "ask", "deny"], "description": "Policy for opening applications, files, or folders through the operating system." },
        "developerAgent.permissions.fileScan": { "type": "string", "default": "ask", "enum": ["allow", "ask", "deny"], "description": "Policy for scanning directories outside the workspace." },
        "developerAgent.permissions.desktopControl": { "type": "string", "default": "ask", "enum": ["allow", "ask", "deny"], "description": "Policy for focusing/closing windows and interacting with accessible desktop controls." },
        "developerAgent.execution.timeoutMs": { "type": "number", "default": 600000, "minimum": 30000, "maximum": 1800000, "description": "Maximum execution time for a single approved plan. Full-drive file scans may take several minutes." },
        "developerAgent.permissions.network": { "type": "string", "default": "ask", "enum": ["allow", "ask", "deny"], "description": "External HTTP policy." }
      }
    }
  },
  "scripts": {
    "start": "node src/index.js",
    "runtime": "node runtime/server.js",
    "test": "node --test"
  }
}
